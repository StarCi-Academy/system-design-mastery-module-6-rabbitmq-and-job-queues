name: bullmq-job-queues-java

services:
  redis:
    image: redis:7-alpine
    container_name: m5l1-java-redis
    ports:
      - "6379:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 5s
      retries: 20

  job-api:
    build:
      context: ..
      dockerfile: Dockerfile
    container_name: m5l1-java-job-api
    environment:
      PORT: "3000"
      REDIS_HOST: redis
      REDIS_PORT: "6379"
      FAIL_RATE: "0.3"
      PROCESS_DELAY_MS: "500"
      JOBRUNR_WORKER: "false"
    ports:
      - "3000:3000"
    depends_on:
      redis:
        condition: service_healthy

  worker:
    build:
      context: ..
      dockerfile: Dockerfile
    container_name: m5l1-java-worker
    environment:
      PORT: "3001"
      REDIS_HOST: redis
      REDIS_PORT: "6379"
      FAIL_RATE: "0.3"
      PROCESS_DELAY_MS: "500"
      JOBRUNR_WORKER: "true"
      WORKER_CONCURRENCY: "1"
    depends_on:
      redis:
        condition: service_healthy
